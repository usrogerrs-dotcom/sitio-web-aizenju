<!doctype html>
<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>PC para estudiantes • Aizenju</title>
        <meta name="description" content="Herramientas tecnológicas personalizadas para romper tus límites." />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet" />
    </head>
    <body>
        <header class="site-header">
            <nav class="nav" aria-label="Principal">
                <a class="logo" href="/" aria-label="Inicio Aizenju">
                    <!-- Logo inline para controlar color con CSS (usa currentColor) -->
                    <svg class="logo-icon" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Aizenju">
                        <path fill="currentColor" d="M 50 10 L 10 90 L 30 90 L 50 45 L 70 90 L 90 90 Z" />
                        <path fill="currentColor" d="M 30 90 C 35 98, 65 98, 70 90 L 70 90 L 30 90 Z" />
                    </svg>
                </a>
                <button class="nav__toggle" aria-label="Abrir menú" aria-controls="site-menu" aria-expanded="false">
                    <span class="nav__bar"></span>
                    <span class="nav__bar"></span>
                    <span class="nav__bar"></span>
                </button>
                <ul id="site-menu" class="menu" role="menubar">
                    <li><a href="/setups">Setups de PC</a></li>
                    <li><a href="/servicio-tecnico">Servicio Técnico</a></li>
                    <li><a href="/reciclaje-laptops">Reciclaje de laptops</a></li>
                </ul>
            </nav>
            <div class="nav__backdrop" hidden></div>
        </header>
        <main>
            <slot />
        </main>
        <footer class="site-footer">
            <small> {new Date().getFullYear()} Aizenju. Todos los derechos reservados.</small>
        </footer>
    </body>
    <style>
        :root {
            /* Dark default */
            --bg: #0a0a0a;
            --text: #e5e7eb;
            --muted: #9ca3af;
            --accent: #7c3aed;
            --accent-2: #06b6d4;
            --card: #111214;
            --border: rgba(255,255,255,0.06);
        }

        html,
        body {
            margin: 0;
            width: 100%;
            height: 100%;
            background: var(--bg);
            color: var(--text);
            font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Noto Sans", "Apple Color Emoji", "Segoe UI Emoji";
        }

        /* Permite que los controles del navegador se adapten al tema */
        html { color-scheme: dark light; }

        main { min-height: 80vh; padding-top: 64px; }

        .site-header {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 50;
            backdrop-filter: blur(8px);
            background: linear-gradient(to bottom, rgba(0,0,0,0.55), rgba(0,0,0,0.15));
            border-bottom: 1px solid var(--border);
            min-height: 56px;
        }

        /* Estado scrolled: header más sólido y color de marca en el logo */
        .site-header.scrolled { background: rgba(0,0,0,0.65); }
        .site-header.scrolled .logo { color: var(--accent); }

        .nav {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 10px 20px;
        }

        .logo { display: flex; align-items: center; text-decoration: none; color: #e5e7eb; }
        .logo-icon { height: 20px; width: auto; display: block; }
        .logo:hover { color: #ffffff; }

        .menu {
            display: flex;
            list-style: none;
            gap: 20px;
            margin: 0;
            padding: 0;
        }

        .menu a {
            color: var(--text);
            text-decoration: none;
            font-weight: 500;
            opacity: 0.9;
            transition: opacity 0.2s ease, color 0.2s ease;
        }

        .menu a:hover { opacity: 1; color: white; }

        .site-footer {
            border-top: 1px solid var(--border);
            padding: 40px 20px;
            text-align: center;
            color: var(--muted);
            background: #0b0c0e;
        }

        /* Light mode */
        @media (prefers-color-scheme: light) {
            :root {
                --bg: #ffffff;
                --text: #111827;
                --muted: #4b5563;
                --card: #f7f7f9;
                --border: rgba(0,0,0,0.08);
                /* accents conservan la marca */
            }
            .site-footer { background: #f9fafb; }
            .site-header.scrolled .logo { color: var(--accent); }
        }

        /* Toggle (hamburguesa) */
        .nav__toggle {
            display: none;
            width: 40px; height: 40px;
            background: none; border: 1px solid var(--border); border-radius: 10px;
            color: inherit; cursor: pointer;
            align-items: center; justify-content: center; gap: 3px; flex-direction: column;
        }
        .nav__bar { display: block; width: 20px; height: 2px; background: currentColor; border-radius: 2px; }
        .nav__backdrop { position: fixed; inset: 0; background: rgba(0,0,0,0.45); backdrop-filter: blur(2px); display: none; }
        .site-header.is-open + .nav__backdrop, .site-header.is-open ~ .nav__backdrop { display: block; }

        /* Menú móvil */
        @media (max-width: 900px) {
            .nav { padding: 10px 14px; }
            .nav__toggle { display: inline-flex; }
            .menu {
                position: absolute; top: 56px; right: 14px;
                display: grid; gap: 8px; padding: 12px; margin: 0;
                background: var(--card); border: 1px solid var(--border); border-radius: 12px;
                box-shadow: 0 20px 50px rgba(0,0,0,0.45);
                min-width: 220px;
                transform-origin: top right; transform: scale(0.95); opacity: 0; pointer-events: none;
                transition: transform 160ms ease, opacity 160ms ease;
            }
            .site-header.is-open .menu { transform: scale(1); opacity: 1; pointer-events: auto; }
        }

        @media (max-width: 720px) {
            .menu { gap: 12px; overflow-x: auto; -webkit-overflow-scrolling: touch; scrollbar-width: none; }
            .menu::-webkit-scrollbar { display: none; }
            .logo-icon { height: 16px; }
            main { padding-top: 58px; }
        }
    </style>
    <script>
        // Añadir clase 'scrolled' al header cuando hay desplazamiento
        (function(){
            const header = document.querySelector('.site-header');
            if (!header) return;
            const onScroll = () => {
                if (window.scrollY > 8) header.classList.add('scrolled');
                else header.classList.remove('scrolled');
            };
            window.addEventListener('scroll', onScroll, { passive: true });
            onScroll();
        })();

        // Menú hamburguesa accesible
        (function(){
            const header = document.querySelector('.site-header');
            const toggle = document.querySelector('.nav__toggle');
            const menu = document.getElementById('site-menu');
            const backdrop = document.querySelector('.nav__backdrop');
            if (!header || !toggle || !menu || !backdrop) return;

            const openMenu = () => {
                header.classList.add('is-open');
                toggle.setAttribute('aria-expanded', 'true');
                backdrop.hidden = false;
                // Foco al primer enlace del menú
                const first = menu.querySelector('a');
                if (first) first.focus();
                document.body.style.overflow = 'hidden';
            };
            const closeMenu = () => {
                header.classList.remove('is-open');
                toggle.setAttribute('aria-expanded', 'false');
                backdrop.hidden = true;
                toggle.focus();
                document.body.style.overflow = '';
            };

            toggle.addEventListener('click', () => {
                const expanded = toggle.getAttribute('aria-expanded') === 'true';
                if (expanded) closeMenu(); else openMenu();
            });
            backdrop.addEventListener('click', closeMenu);
            document.addEventListener('keydown', (e) => {
                if (e.key === 'Escape' && header.classList.contains('is-open')) closeMenu();
            });
            // Cerrar al navegar por un enlace del menú
            menu.addEventListener('click', (e) => {
                const a = (e.target && e.target.closest) ? e.target.closest('a') : null;
                if (a) closeMenu();
            });
        })();
    </script>
</html>
