<!doctype html>
<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>PC para estudiantes • Aizenju</title>
        <meta name="description" content="Herramientas tecnológicas personalizadas para romper tus límites." />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet" />
    </head>
    <body>
        <header class="site-header">
            <nav class="nav">
                <a class="logo" href="/" aria-label="Inicio Aizenju">
                    <!-- Logo inline para controlar color con CSS (usa currentColor) -->
                    <svg class="logo-icon" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Aizenju">
                        <path fill="currentColor" d="M 50 10 L 10 90 L 30 90 L 50 45 L 70 90 L 90 90 Z" />
                        <path fill="currentColor" d="M 30 90 C 35 98, 65 98, 70 90 L 70 90 L 30 90 Z" />
                    </svg>
                </a>
                <ul class="menu">
                    <li><a href="/setups">Setups de PC</a></li>
                    <li><a href="/servicio-tecnico">Servicio Técnico</a></li>
                    <li><a href="/reciclaje-laptops">Reciclaje de laptops</a></li>
                </ul>
            </nav>
        </header>
        <main>
            <slot />
        </main>
        <footer class="site-footer">
            <small> {new Date().getFullYear()} Aizenju. Todos los derechos reservados.</small>
        </footer>
    </body>
    <style>
        :root {
            --bg: #0a0a0a;
            --text: #e5e7eb;
            --muted: #9ca3af;
            --accent: #7c3aed;
            --accent-2: #06b6d4;
            --card: #111214;
        }

        html,
        body {
            margin: 0;
            width: 100%;
            height: 100%;
            background: var(--bg);
            color: var(--text);
            font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Noto Sans", "Apple Color Emoji", "Segoe UI Emoji";
        }

        main { min-height: 80vh; }

        .site-header {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 50;
            backdrop-filter: blur(8px);
            background: linear-gradient(to bottom, rgba(0,0,0,0.55), rgba(0,0,0,0.15));
            border-bottom: 1px solid rgba(255,255,255,0.06);
        }

        /* Estado scrolled: header más sólido y color de marca en el logo */
        .site-header.scrolled { background: rgba(0,0,0,0.65); }
        .site-header.scrolled .logo { color: var(--accent); }

        .nav {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 10px 20px;
        }

        .logo { display: flex; align-items: center; text-decoration: none; color: #e5e7eb; }
        .logo-icon { height: 20px; width: auto; display: block; }
        .logo:hover { color: #ffffff; }

        .menu {
            display: flex;
            list-style: none;
            gap: 20px;
            margin: 0;
            padding: 0;
        }

        .menu a {
            color: var(--text);
            text-decoration: none;
            font-weight: 500;
            opacity: 0.9;
            transition: opacity 0.2s ease, color 0.2s ease;
        }

        .menu a:hover { opacity: 1; color: white; }

        .site-footer {
            border-top: 1px solid rgba(255,255,255,0.06);
            padding: 40px 20px;
            text-align: center;
            color: var(--muted);
            background: #0b0c0e;
        }

        @media (max-width: 720px) {
            .menu { gap: 14px; }
            .logo-icon { height: 16px; }
        }
    </style>
    <script>
        // Añadir clase 'scrolled' al header cuando hay desplazamiento
        (function(){
            const header = document.querySelector('.site-header');
            if (!header) return;
            const onScroll = () => {
                if (window.scrollY > 8) header.classList.add('scrolled');
                else header.classList.remove('scrolled');
            };
            window.addEventListener('scroll', onScroll, { passive: true });
            onScroll();
        })();
    </script>
</html>
